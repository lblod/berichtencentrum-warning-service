{
  "name": "berichtencentrum-warning-service",
  "version": "0.0.2",
  "description": "Service that generates warning emails when no messages are sent through the berichtencentrum during the day",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lblod/berichtencentrum-warning-service.git"
  },
  "author": "lblod",
  "license": "MIT",
  "dependencies": {
    "@lblod/mu-auth-sudo": "^0.2.0",
    "cron": "^1.8.2"
  }
}
